{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS", "buildCommand": "npm ci" },
  "deploy": {
    "startCommand": "npx tsx hedera/service/payer-api.ts",
    "healthcheckPath": "/status",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
