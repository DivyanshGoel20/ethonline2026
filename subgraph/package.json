{
  "name": "float-subgraph",
  "version": "0.1.0",
  "description": "The Graph Subgraph indexing Float agent credit events, financial profiles, and payment activities",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
